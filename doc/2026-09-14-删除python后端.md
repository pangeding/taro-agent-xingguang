# 2026-09-14 技术文档：删除 Python 后端，专注 Go 后端

## 0. 背景与目标

- `backend/` 为 Python（FastAPI + LangGraph + Peewee）旧后端，已由 `backend-go/`（Go + Gin + GORM + Eino）完整替代。
- `backend-go/` 为 Python 版功能超集，API 路径、请求/响应结构完全兼容（见 `doc/2026-07-07-backend-go-migration.md`）。
- 前端仅连接 `http://localhost:8000/api/v1`，两个后端均监听 `:8000`，故前端无需改动。
- 目标：物理删除 `backend/`，并将所有指向 Python 的运行时/说明文件同步改为 Go。

## 1. 前置校验（已确认）

- `backend/data/tarot_cards.json` 与 `backend-go/data/tarot_cards.json` md5 一致：
  `64198aab54f951fdcaab3d1ab17295e8`，删除无数据丢失风险。
- `backend/` 188M，其中 187M 为未跟踪的 `.venv`；git 跟踪 36 个文件。
- `backend-go/` 已在 git 跟踪中，含 `cmd/ server main.go`、`internal/`、`scripts/import_cards/main.go`、`START.md`、`.env.example`。

## 2. 删除操作

```bash
cd /home/pangding/ai/taro_agent
git rm -r backend
rm -rf backend        # 清掉未跟踪的 .venv / .langgraph_api / dist / .env / .claude
```

## 3. 文件改动清单

### 3.1 Makefile

将后端相关目标改为 Go 命令，保留目标名以兼容习惯用法：

- `install-backend`: `cd backend-go && go mod download`
- `init-db` / `import-cards`: 均执行 `cd backend-go && go run scripts/import_cards/main.go`（该脚本自动建库建表并导入，幂等）
- `backend`: `cd backend-go && go run cmd/server/main.go`
- `dev`: Go 后端与前端并行启动
- `clean`: 删除 `backend-go/server` 二进制；移除 Python 清理项（`__pycache__`、`*.pyc`、`backend/taro.db`、`backend/.venv`）
- `help`: 文案同步为 Go。

### 3.2 .gitignore

删除 Python 专属条目：

```
.venv/
__pycache__/
*.pyc
.langgraph_api/
```

保留：`.claude/`、`.env`、`node_modules/`、`.next/`、`*.db*`、`server`。

### 3.3 README.md

- 技术栈：Go 1.22 / Gin / GORM / Eino / SQLite / DashScope(OpenAI 兼容)。
- 快速开始：改为 `backend-go` 步骤（`cp .env.example .env` → `go run scripts/import_cards/main.go` → `go run cmd/server/main.go`）。
- 项目结构、数据库路径更新为 `backend-go/data/taro.db`。
- 删除已不存在的 `data/`、`scripts/` 根目录描述。

### 3.4 AGENTS.md

第 0 条改为专注 Go 后端（`backend-go/`），移除“Python 版本在考虑废弃”的表述。

### 3.5 TODO.md

删除明显无意义的 Python 专属条目（如“完整python方案”），保留带 `【done】` 的迁移历史。

## 4. 明确不改动

- `doc/` 下历史技术文档全部保留，作为历史记录。
- `spec/` 目录不改动。
- `frontend/` 不改动。

## 5. 验证

```bash
cd /home/pangding/ai/taro_agent
# 1. 无运行时引用残留（忽略历史文档与 Go 自身）
rg -n "backend/" -g '!doc/**' -g '!backend-go/**' -g '!frontend/node_modules/**'
# 2. Go 编译
cd backend-go && go build ./...
# 3. Makefile 命令预演
make -n backend
make -n import-cards
# 4. git 状态
git status --short
```
